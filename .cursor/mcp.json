{
  "mcpServers": {
    "render": {
      "url": "https://mcp.render.com/mcp",
      "envFile": "${workspaceFolder}/.env",
      "headers": {
        "Authorization": "Bearer ${env:RENDER_API_KEY}"
      }
    }
  }
}
